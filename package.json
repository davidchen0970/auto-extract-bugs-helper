{
  "name": "bug-tarball-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side tar/tar.gz parser + Coverity / BlackDuck defect preview workbench",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
